[package]
name = "emacs-lua"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
mlua = { version = "0.12", features = ["luajit", "vendored"] }
emacs-core = { path = "../core" }
anyhow.workspace = true
